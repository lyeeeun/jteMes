<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtMapper">

	<sql id="item_base">
			item.item_id as itemId,
			item.item_nm as itemNm,
			item.item_type as itemType,
			item.item_price as itemPrice,
			item.item_mtrl_cost as itemMtrlCost,
			item.item_person_cost as itemPersonCost,
			item.item_std01 as itemStd01,
			item.item_std02 as itemStd02,
			item.item_std03 as itemStd03,
			item.item_std04 as itemStd04,
			item.item_std05 as itemStd05,
			item.item_std_str01 as itemStdStr01,
			item.item_std_str02 as itemStdStr02,
			place.place_nm		AS placeNm,
			item.item_std_str03 as itemStdStr03,
			item.item_std_str04 as itemStdStr04,
			item.item_std_str05 as itemStdStr05,
			item.item_unit as itemUnit,
			item.item_mtrl as itemMtrl,
			item.item_total_stock as itemTotalStock,
			item.item_day_target as itemDayTarget,
			item.description,
			item.is_use as isUse,
			item.created_at as createdAt,
			item.creator_id as creatorId,
			item.updated_at as updatedAt,
			item.updator_id as updatorId,
			item_deduction as itemDeduction
		FROM bc_item_info item
		LEFT OUTER JOIN bc_place_info place ON item.item_std_str02 = place.place_id
		WHERE 1=1
			<if test="itemId != null and itemId !='' ">
				AND item.item_id = #{itemId}
			</if>
			<if test="itemType != null and itemType != '' ">
				AND item.item_type = #{itemType}
			</if>
			<if test="itemStdStr02 != null and itemStdStr02 !='' ">
				AND item.item_std_str02 = #{itemStdStr02}
			</if>		
			<if test="isUse != null and isUse != '' ">
				AND item.is_use = #{isUse}
			</if>
			<choose>
				<when test="searchGubun == 'itemId'">
					AND item.item_id LIKE CONCAT('%',#{searchText},'%')
				</when>
				<when test="searchGubun == 'itemNm'">
					AND item.item_nm LIKE CONCAT('%',#{searchText},'%')
				</when>
			</choose>
	</sql>
 	<select id="selectItemList"
		parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto"
		resultType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto">
		SELECT *
		FROM (
			SELECT
			<choose>
				<when test="sort != null and sort != '' ">
					ROW_NUMBER() OVER(ORDER BY ${sort}) AS RNUM,
				</when>
				<otherwise>
					ROW_NUMBER() OVER(ORDER BY updatedAt DESC) AS RNUM,
				</otherwise>
			</choose> 
				<include refid="item_base"/>
			)s_table
			<if test="firstIndex != null and firstIndex != 0 and lastIndex != null and lastIndex != 0 ">
				WHERE RNUM <![CDATA[ <= ]]> #{lastIndex} AND RNUM <![CDATA[ >= ]]> #{firstIndex}
			</if>
	</select>
 	
	<select id="rowCount"
		parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto"
		resultType="int">
		SELECT COUNT(*)
		FROM(
			SELECT
			<include refid="item_base"/>
		)c_table
	</select>
	
	<select id="selectRtlCompList"
		parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto"
		resultType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto">
		SELECT * 
		FROM bc_rtl_item_comp a
		LEFT JOIN bc_company_info b
		ON a.comp_id = b.comp_id
		WHERE item_id = #{itemId}
	</select>
	
	<select id="selectRtlCompCount"
		parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto"
		resultType="int">
		SELECT COUNT(*) 
		FROM bc_rtl_item_comp a
		LEFT JOIN bc_company_info b
		ON a.comp_id = b.comp_id
		WHERE item_id = #{itemId}
	</select>
	
	<insert id="insertItem" parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto">
		<selectKey keyProperty="itemId" resultType="String" order="BEFORE">
			SELECT CREATE_PK('ITEM') as itemId
		</selectKey>
		INSERT INTO bc_item_info
		(
			item_id,
			item_nm,
			item_price,
			item_mtrl_cost,
			item_person_cost,
			item_type,
			item_std01,
			item_std02,
			item_std03,
			item_std04,
			item_std05,
			item_std_str01,
			item_std_str02,
			item_std_str03,
			item_std_str04,
			item_std_str05,
			item_unit,
			item_mtrl,
			item_total_stock,
			item_day_target,
			description,
			is_use,
			created_at,
			creator_id,
			updated_at,
			updator_id,
			item_deduction
		)
		VALUES
		(
			#{itemId},
			#{itemNm},
			#{itemPrice},
			#{itemMtrlCost},
			#{itemPersonCost},
			#{itemType},
			#{itemStd01},
			#{itemStd02},
			#{itemStd03},
			#{itemStd04},
			#{itemStd05},
			#{itemStdStr01},
			#{itemStdStr02},
			#{itemStdStr03},
			#{itemStdStr04},
			#{itemStdStr05},
			#{itemUnit},
			#{itemMtrl},
			#{itemTotalStock},
			#{itemDayTarget},
			#{description},
			TRUE,
			now(),
			#{creatorId},
			now(),
			#{updatorId},
			#{itemDeduction}
		)
	</insert>
	
	<update id="updateItem"
		parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto">
		UPDATE bc_item_info
		SET
			item_nm = #{itemNm},
			item_price = #{itemPrice},
			item_mtrl_cost = #{itemMtrlCost},
			item_person_cost = #{itemPersonCost},
			item_type = #{itemType},
			item_std01 = #{itemStd01},
			item_std02 = #{itemStd02},
			item_std03 = #{itemStd03},
			item_std04 = #{itemStd04},
			item_std05 = #{itemStd05},
			item_std_str01 = #{itemStdStr01},
			item_std_str02 = #{itemStdStr02},
			item_std_str03 = #{itemStdStr03},
			item_std_str04 = #{itemStdStr04},
			item_std_str05 = #{itemStdStr05},
			item_unit = #{itemUnit},
			item_mtrl = #{itemMtrl},
			item_total_stock = #{itemTotalStock},
			item_day_target = #{itemDayTarget},
			description = #{description},
			updated_at = now(),
			updator_id = #{updatorId},
			item_deduction = #{itemDeduction}
			<if test="itemTotalStock != null">
				,item_total_stock = #{itemTotalStock}
			</if>
			<if test="itemDayTarget != null">
				,item_day_target = #{itemDayTarget}
			</if>
			<if test="itemStd05 != null">
				,item_std05 = #{itemStd05}
			</if>
		WHERE item_id = #{itemId}
	</update>
	
	<update id="updateItemDeduction"
		parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto">
		UPDATE bc_item_info
		SET
			item_deduction = #{itemDeduction}
		WHERE item_id = #{itemId}
	</update>
	
	<insert id="insertRtlComp" parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto">
		INSERT INTO bc_rtl_item_comp
		(
			item_id,
			comp_id
		)
		VALUES
		(
			#{itemId},
			#{compId}
		)
	</insert>
	
	<delete id="deleteRtlComp" parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto">
		DELETE FROM bc_rtl_item_comp
		WHERE 1=1
		AND item_id = #{itemId}
		AND comp_id = #{compId} 
	</delete>
	
	<update id="updateIsUse"
		parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto">
		UPDATE bc_item_info
		SET is_use = #{isUse}
		WHERE item_id = #{itemId}
	</update>
	
	<insert id="insertKpi" parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto">
		INSERT INTO rpt_kpi_info
		(
			item_id,
			kpi_target,
			kpi_date,
			kpi_value
		)
		VALUES
		(
			#{itemId},
			#{kpiTarget},
			#{kpiDate},
			#{kpiValue}
		)
	</insert>
	
	<select id="selectKpi"
	parameterType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto"
	resultType="jin.mes.cform.basMgt.operMgt.itemMgt.NewItemMgtDto">
		SELECT
			kpi_target AS kpiTarget,
			CONCAT(YEAR(kpi_date),'-',MONTH(kpi_date)) AS kpiDate,
			kpi_value AS kpiValue
		FROM rpt_kpi_info
		WHERE item_id = #{itemId}
		GROUP BY CONCAT(YEAR(kpi_date),'-',MONTH(kpi_date))
		ORDER BY kpi_date
		
<!-- 		SELECT 
			kpi_target AS kpiTarget,
			CONCAT(YEAR(kpi_date),'-',MONTH(kpi_date)) AS kpiDate,
			kpi_value AS kpiValue
		FROM rpt_kpi_info
		WHERE item_id = #{itemId}
		UNION
		SELECT 
			'kpi_stock' AS kpiTarget,
			concat(year(kpiDate),'-',month(kpiDate)) as kpiDate,
			AVG(kpiValue)
		FROM
		(
			SELECT 
				
				DATE(item_qty_date) AS kpiDate,
				kpiValue
			FROM
			(
				SELECT 
				item_qty_date,
				SUM(item_qty_total) OVER(ORDER BY item_qty_date ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS kpiValue
				FROM qm_item_mgt
				WHERE item_id = #{itemId} 
				ORDER BY item_qty_date DESC
			) sub
			GROUP BY kpiDate
		) SUB
		GROUP BY concat(year(kpiDate),'-',month(kpiDate))
		ORDER BY kpiDate ASC -->
	</select>
	
</mapper>